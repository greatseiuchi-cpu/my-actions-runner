name: Bash Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    defaults:
      run:
        shell: bash
    
    steps:
      - name: Test Bash
        run: |
          echo "Testing Bash runner"
          pwd
          ls -la
          echo "Current user: $USER"
          echo "Current path: $PATH"
